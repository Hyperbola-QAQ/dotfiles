# Extra autostart processes
# exec-once = uwsm-app -- my-service

exec-once = omarchy-lock-screen

#exec-once = uwsm-app -- awww-daemon
#exec-once = uwsm-app -- awww img ~/.config/omarchy/current/background

exec-once = sleep 1 && hyprctl setcursor miku-cursor-linux 48
exec-once = dpi=$(awk "BEGIN {print (${GDK_SCALE:-1}) * 96}");echo "Xft.dpi: $dpi" | xrdb -merge

#exec-once = sh -c 'exec uwsm-app -- linuxqq --ozone-platform-hint=x11 --force-device-scale-factor="${GDK_SCALE:-1}"'
# 注释原来的缩放避免双倍缩放
#exec-once = uwsm-app -- wechat #env QT_SCALE_FACTOR=${GDK_SCALE:-1} uwsm-app -- wechat
